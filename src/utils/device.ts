// src/utils/device.ts
// Placeholder utility for device / performance detection.

export function isLowEndDevice(): boolean {
  // TODO: Implement robust heuristics (UA parsing + hardware concurrency + memory).
  // For now, return false to always load full experience.
  return false;
}